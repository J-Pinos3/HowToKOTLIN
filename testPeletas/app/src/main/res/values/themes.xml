<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TestPeletas" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TestPeletas" parent="Base.Theme.TestPeletas" />


    <!-- chipEntry -->
    <style name="chEntry" parent="Widget.MaterialComponents.Chip.Entry">
        <item name="android:textStyle">  bold  </item>
        <!-- de esta manera el texto siempre era blanco -->
        <!-- <item name="android:textColor">  @color/white  </item> -->
        <item name="android:textColor"> @drawable/ch_textcolor </item>
        <item name="chipBackgroundColor">  @color/blue_light  </item>
        <item name="chipStrokeColor">@color/blue_dark</item>
        <item name="chipStrokeWidth">2dp</item>

    </style>
</resources>